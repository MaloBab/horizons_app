### Backend Horizons

run `.\env\Scripts\activate` to open the virtual environnement.

run `pip freeze > requirements.txt` into the virtual environnemnt to update or create the requirements file.

run `pip install -r requirements.txt` to install the dependencies of the project.

run `uvicorn app.main:app --reload` to launch the backend.
